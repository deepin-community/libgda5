<?xml version="1.0" encoding="utf-8"?>
<page xmlns="http://projectmallard.org/1.0/" type="topic" id="transactions" xml:lang="sl">
  <info>
    <title type="sort">1</title>
    <link type="topic" xref="query-execution-perspective"/>
    <link type="guide" xref="index#connections"/>
  </info>
  <title>Transakcije</title>
  <p>Prenos je lahko začet, uveljavljen ali povrnjen na osnovi vsake povezave:</p>
  <list>
    <item><p>uporabite <media type="image" mime="image/png" src="figures/trans-begin.png"/> gumb orodne vrstice za začetek prenosa.</p></item>
    <item><p>uporabite <media type="image" mime="image/png" src="figures/trans-commit.png"/> gumb orodne vrstice za uveljavitev prenosa (uveljavi vse spremembe).</p></item>
    <item><p>uporabite <media type="image" mime="image/png" src="figures/trans-rollback.png"/> gumb orodne vrstice za prekinitev prenosa (prezri vse spremembe).</p></item>
  </list>
  <note>
    <p>
      A transaction may be started automatically when executing a SELECT statement which involves
      binary objects (BLOBs), and the transaction may be locked while the resulting data set is
      kept in memory (to get rid of a data set in a
      <link xref="query-execution-perspective">query execution perspective</link>, clear the
      history item relating to the data set).
    </p>
  </note>
</page>
